{
  "image": "ghcr.io/kuleuven-micas/snax:main",
  "postCreateCommand": "apt-get update && apt-get install -y locales && locale-gen en_US.UTF-8 && echo 'export LANG=en_US.UTF-8\nexport LANGUAGE=en_US:en\nexport LC_ALL=en_US.UTF-8' >> /etc/profile",
  "customizations": {
    "vscode": {
      "extensions": [
        "chipsalliance.verible",
        "wavetrace.wavetrace",
        "mshr-h.veriloghdl",
        "ms-python.python",
        "ms-vscode.cpptools"
      ]
    }
  }
}
